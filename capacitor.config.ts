import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.LoveProject.app',
  appName: 'loveMaria',
  webDir: 'dist/projeto-maria/browser'
};

export default config;
